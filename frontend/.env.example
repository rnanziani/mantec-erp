# Copiar a frontend/.env.local para desarrollo o configurar en Render (Static Site)

# URL del backend API (sin barra final). En Render:
# VITE_API_URL=https://mantec-erp.onrender.com
# IMPORTANTE: tras cambiar esta variable, hay que volver a desplegar (Manual Deploy).
VITE_API_URL=http://localhost:3001
